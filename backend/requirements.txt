# Academic Paraphraser - minimal direct dependencies
# Verified for Python 3.14.x. Pip will resolve all transitive deps.

# --- Web framework + ASGI server + SSE streaming ---
fastapi==0.135.3
uvicorn==0.44.0
sse-starlette==3.3.4

# --- Validation ---
pydantic==2.12.5

# --- Configuration / env ---
python-dotenv==1.2.1

# --- OpenAI client (GPT rewrite + Final QA layer) ---
openai==2.3.0

# --- Local HuggingFace models (imported at module level by pipeline.py) ---
# Note: For CPU-only installation, first run:
#   pip install --index-url https://download.pytorch.org/whl/cpu torch==2.8.0
# Then:
#   pip install -r requirements.txt
transformers==4.57.6
torch==2.8.0
